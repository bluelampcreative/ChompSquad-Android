package com.bluelampcreative.chompsquad.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand palette ─────────────────────────────────────────────────────────────
// Derived from app icon radial gradient and logotype artwork.
val Brand_Green_Dark       = Color(0xFF2C7A2C)  // icon gradient dark stop
val Brand_Green            = Color(0xFF4AA448)  // icon gradient midpoint — primary action colour
val Brand_Green_Mid        = Color(0xFF6BBF6B)  // logotype fill / on-dark accent
val Brand_Green_Light      = Color(0xFF72C472)  // icon gradient light stop
val Brand_Green_Container  = Color(0xFFD4EDDA)  // chips, recipe tile tint, tag backgrounds
val Brand_Green_OnDark     = Color(0xFF0A2C0A)  // text on primary in dark theme
// Brand pink retained for promo / marketing material — not a primary UI colour.
val Brand_Pink             = Color(0xFFE84A8A)

// ── Tertiary / debug ──────────────────────────────────────────────────────────
// Amber is used exclusively for debug / developer UI (badge, Environment label).
val Debug_Amber            = Color(0xFFD97706)
val Debug_Amber_Container  = Color(0xFFFEF3C7)
val Debug_Amber_On         = Color(0xFF431407)

// ── Neutral ───────────────────────────────────────────────────────────────────
val Neutral_Background     = Color(0xFFF2F2F7)  // iOS-equivalent system background
val Neutral_Surface        = Color(0xFFFFFFFF)
val Neutral_SurfaceVariant = Color(0xFFDDE5D8)
val Neutral_OnSurface      = Color(0xFF1C1B1F)
val Neutral_OnSurfaceVar   = Color(0xFF414940)
val Neutral_Subtle         = Color(0xFF6B7280)  // captions, secondary text
val Neutral_Outline        = Color(0xFF717970)
val Neutral_OutlineVariant = Color(0xFFC1C9BC)

// ── Error ─────────────────────────────────────────────────────────────────────
val Error_Red              = Color(0xFFB3261E)
val Error_Red_Container    = Color(0xFFF9DEDC)
val Error_Red_On           = Color(0xFF410E0B)

// ── Dark-mode neutrals ────────────────────────────────────────────────────────
val Dark_Background        = Color(0xFF1A1C19)
val Dark_OnBackground      = Color(0xFFE2E3DD)
val Dark_SurfaceVariant    = Color(0xFF414940)
val Dark_Outline           = Color(0xFF8B9388)
